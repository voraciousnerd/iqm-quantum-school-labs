IQM Summer Quantum School Lab Sessions

Note: The original notebooks have been slightly modified, in order to include all necessary packages in the ```requirements.txt``` file.

### Requirements 
Install the necessary packages as shown, preferably in a venv: 
```bash 
pip install --upgrade pip
pip install --no-cache-dir --force-reinstall --upgrade -r requirements.txt
```
